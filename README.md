# Vertex Workstation

A host shell for two detachable Vertex units:

- RAY — observation sensor
- FORGE — mutation engine

This foundation intentionally contains no real RAY or FORGE execution logic yet.

The first milestone is the construction boundary itself:
Vue 3 + TypeScript + Tauri/Rust + detachable unit contracts.

The next milestone is FORGE end-to-end proof.
