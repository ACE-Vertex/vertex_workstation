{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Vertex Workstation host shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
